#include <bits/stdc++.h>
using namespace std;
const int N = 1e5;
int main() {

   int a , b , c;
   cin >> a >> b >> c;
   swap (a , b);
   swap(a , c);
   cout << a << ' ' << b << ' ' << c;
    return 0;
}